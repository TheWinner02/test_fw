name: Make OneUI 7.0 Port for Universal7885

on:
  workflow_dispatch:
    inputs:
      firmware_url:
        description: "Inserisci l'URL diretto del firmware EXYNOS (.zip o .tar.md5)"
        required: true
        type: string
      fs_type:
        description: "Specifica il tipo di filesystem (ext4, f2fs o erofs)"
        required: true
        default: "ext4"
        type: string

jobs:
  OneUI7_Auto_Port_Universal7885:
    runs-on: ubuntu-latest

    steps:
    - name: 📦 Checkout Repository
      uses: actions/checkout@v4

    # 🧹 PULIZIA E SPAZIO
    - name: 🔧 Libera spazio su disco
      run: |
        echo "🧹 Rimozione pacchetti non necessari..."
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        sudo docker system prune -a -f
        sudo apt clean
        df -h /

    - name: 📊 Crea funzione monitor_spazio
      run: |
        echo '#!/bin/bash
        FREE=$(df --output=avail -BG / | tail -1 | sed "s/G//")
        echo "📊 Spazio libero: ${FREE} GB"
        if [ "$FREE" -lt 2 ]; then
          echo "❌ ERRORE: spazio insufficiente (<2 GB)"
          exit 1
        fi' | sudo tee /usr/local/bin/monitor_spazio > /dev/null
        sudo chmod +x /usr/local/bin/monitor_spazio

    # ⚙️ INSTALLAZIONE DIPENDENZE
    - name: ⚙️ Installa dipendenze necessarie
      run: |
        sudo apt update
        DEBIAN_FRONTEND=noninteractive sudo apt install -yq \
          unzip lz4 android-sdk-libsparse-utils attr zip wget git tar f2fs-tools erofs-utils e2fsprogs python3 brotli rsync fuse3
        monitor_spazio

    # 📥 DOWNLOAD FIRMWARE
    - name: 📥 Scarica firmware
      run: |
        mkdir -p ACK
        echo "➡️ Download del firmware da: ${{ github.event.inputs.firmware_url }}"
        curl -L "${{ github.event.inputs.firmware_url }}" -o ACK/base.zip --progress-bar
        ls -lh ACK
        monitor_spazio

    # 📦 ESTRAZIONE ZIP/TAR.MD5
    - name: 📦 Estrai archivio principale
      run: |
        cd ACK
        if file base.zip | grep -q "gzip compressed"; then
          echo "📦 File TAR.MD5 rilevato, estrazione..."
          tar -xf base.zip
        else
          echo "📦 File ZIP rilevato, estrazione..."
          unzip base.zip
        fi
        rm -f base.zip
        echo "✅ Estrazione completata"
        monitor_spazio

    - name: 🔹 Estrai file .tar.md5
      run: |
        cd ACK
        echo "📂 Estrazione di .tar.md5"
        tar --wildcards -xf *.tar.md5 'super.*.*'
        rm -f *.tar.md5
        echo "✅ File TAR.MD5 estratti"
        monitor_spazio

    # 🔸 DECOMPRESSIONE SUPER.IMG.LZ4
    - name: 🔸 Decomprimi super.img.lz4
      run: |
        cd ACK
        if [ -f super.img.lz4 ]; then
          echo "🔹 Decompressione super.img.lz4"
          lz4 -d super.img.lz4 super.img.sparse
          rm -f super.img.lz4
        fi
        monitor_spazio

    # 🔁 CONVERSIONE SPARSE → RAW
    - name: 🔁 Converti sparse in raw
      run: |
        cd ACK
        echo "🔁 Conversione super.img.sparse → super.img"
        simg2img super.img.sparse super.img
        rm -f super.img.sparse
        ls -lh super.img
        monitor_spazio

    # 🧰 LPUNPACK
    - name: 🧰 Scarica e prepara lpunpack
      run: |
        git clone https://github.com/unix3dgforce/lpunpack.git
        monitor_spazio

    # 🗂️ ESTRAZIONE PARTIZIONI
    - name: 🗂️ Estrai partizioni da super.img
      run: |
        cd ACK
        mkdir out
        echo "📂 Estrazione da super.img..."
        python3 ../lpunpack/lpunpack.py -p product,system super.img out/
        rm -f super.img
        ls -lh out
        monitor_spazio

    # 🧩 MONTA SYSTEM E PRODUCT IN BASE AL TIPO (fallback su EXT4)
- name: 🧩 Monta system.img e product.img
  run: |
    cd ACK
    TYPE="${{ steps.detect.outputs.type }}"
    echo "⚙️ Tipo rilevato: $TYPE"
    mkdir -p system_mount system_modified product_mount product_modified

    if [ "$TYPE" = "erofs" ]; then
      echo "🔹 Montaggio EROFS tramite fuse..."
      mkdir -p fuse_sys fuse_prod
      erofsfuse out/system.img fuse_sys
      erofsfuse out/product.img fuse_prod
      sudo rsync -aHAX fuse_sys/ system_modified/
      sudo rsync -aHAX fuse_prod/ product_modified/
      fusermount -u fuse_sys || true
      fusermount -u fuse_prod || true
      rm -rf fuse_sys fuse_prod
    else
      echo "🔹 Montaggio EXT4..."
      sudo mount -t ext4 -o loop out/system.img system_mount || { echo "❌ Mount di system.img fallito"; exit 1; }
      sudo mount -t ext4 -o loop out/product.img product_mount || { echo "❌ Mount di product.img fallito"; exit 1; }

      echo "📂 Copia contenuto..."
      sudo rsync -aHAX system_mount/ system_modified/
      sudo rsync -aHAX product_mount/ product_modified/

      echo "🧹 Smontaggio..."
      sudo umount system_mount
      sudo umount product_mount
      sudo rm -rf system_mount product_mount
    fi

    echo "✅ Contenuto estratto in system_modified/ e product_modified/"
    ls -lah system_modified | head
    monitor_spazio

    # ⚙️ APPLICA MODIFICHE
    - name: ⚙️ Applica modifiche OneUI 7.0
      run: |
        cd ACK
        echo "⚙️ OneUI 7.0 Port AutoBuild..."
        chmod +x ../script/system/ui7_debloat.sh
        sudo bash ../script/system/ui7_debloat.sh

    # 🏗️ RICOMPILA SYSTEM.IMG
    - name: 🏗️ Ricompila system.img in ext4
      run: |
        cd ACK
        echo "⚙️ Ricompilazione filesystem forzata in ext4"
        dd if=/dev/zero of=out/system.img bs=1M count=4096 status=progress
        mkfs.ext4 -F -L system out/system.img

        sudo mkdir -p /mnt/system
        sudo mount -o loop out/system.img /mnt/system
        echo "📂 Copia file in system..."
        sudo rsync -aHAX system_modified/* /mnt/system/
        sudo sync
        sudo umount /mnt/system
        sudo rm -rf /mnt/system system_modified

        echo "🔁 Conversione in sparse image..."
        img2simg out/system.img out/system_sparse.img
        sudo rm -f out/system.img
        ls -lh out/system_sparse.img
        monitor_spazio

    # 🔄 CONVERTI IN .NEW.DAT
    - name: 🔄 Converti system_sparse.img → .new.dat
      run: |
        cd ACK
        git clone https://github.com/xpirt/img2sdat.git
        cd img2sdat
        python3 img2sdat.py ../out/system_sparse.img -o ../out -v 4
        sudo rm -f ../out/system_sparse.img && cd ..
        ls -lh out/system.new.dat
        monitor_spazio

    # 🗜️ COMPRESSIONE OUTPUT
    - name: 🗜️ Comprimi risultati finali
      run: |
        cd ACK
        echo "🗜️ Compressione file..."
        mkdir out/product
        sudo rsync -aHAX product_modified/* out/product/ && sudo rm -rf product_modified
        cd out
        zip -9 ../Port.zip *
        cd .. && sudo rm -rf out
        ls -lh Port.zip
        monitor_spazio

    # ☁️ UPLOAD SU SOURCEFORGE
    - name: 🔐 Configura SSH e carica su SourceForge
      run: |
        cd ACK
        mkdir -p ~/.ssh
        echo "${{ secrets.SF_SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan frs.sourceforge.net >> ~/.ssh/known_hosts
        scp Port.zip thewinner02@frs.sourceforge.net:/home/frs/project/oneui-ports/test/
        echo "✅ Upload completato su SourceForge"
        monitor_spazio
